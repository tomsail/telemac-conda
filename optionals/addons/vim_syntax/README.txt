How to use the vim syntax for steering files and dictionary:

Copy the folders below into you .vim folder (Should be in ~/.vim, if the folders already exist just add the files ftdetect/* and syntax/* into them)

ftdetect
syntax
